# Database URL for Vercel Postgres
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Vercel Blob storage token
BLOB_READ_WRITE_TOKEN=""

# Optional: Claude API key (can also be set in the app UI)
ANTHROPIC_API_KEY=""

# AWS Cognito Authentication
NEXT_PUBLIC_COGNITO_USER_POOL_ID="us-east-1_XXXXXXXXX"
NEXT_PUBLIC_COGNITO_CLIENT_ID="xxxxxxxxxxxxxxxxxxxxxxxxxx"

# Admin users (comma-separated list of email addresses)
ADMIN_EMAILS="admin@yourdomain.com"

# AWS Cognito Admin Setup Instructions:
# 1. Go to AWS Console > Cognito > User Pools
# 2. Create a new User Pool with email sign-in
# 3. Add an app client (no client secret for public web apps)
# 4. Add admin email addresses to ADMIN_EMAILS environment variable
